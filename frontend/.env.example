# Copy to frontend/.env and adjust. Never put secrets in here: every REACT_APP_*
# variable is inlined into the public JS bundle.

# Base URL of the ListingCraft API, WITHOUT the /api suffix (src/lib/api.js
# appends it). Local backend: http://localhost:8001
REACT_APP_BACKEND_URL=http://localhost:8001
